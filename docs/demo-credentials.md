# pApAmA — Demo Login Credentials

Test accounts for reviewing each portal. **The shared password is NOT stored in
this repo** (it was removed after a secret-scan alert). It is set via the
`DEMO_PASSWORD` entry in `.env.local` and shared with reviewers out-of-band
(e.g. over chat/email) — ask the maintainer for the current value.

> These are demo-only accounts on the test environment. Please don't enter real
> personal or payment data.

## Accounts

| Role | Email | Where to sign in | What you'll see |
|---|---|---|---|
| **Admin** | `admin@papama.test` | `/login` | Full admin console — donations, tokens, vendors, beneficiaries, reports |
| **Compliance** | `compliance@papama.test` | `/login` | Admin console scoped to compliance / audit views |
| **Vendor Manager** | `manager@papama.test` | `/login` | Admin console scoped to vendor onboarding & management |
| **Vendor** | `vendor@papama.test` | `/vendor/login` | Vendor app — serve meals, redeem tokens (account pre-approved) |
| **Volunteer** | `volunteer@papama.test` | `/volunteer/login` | Volunteer app — hold & distribute tokens |
| **Donor** | `donor@papama.test` | `/donor/login` | Donor app — donate, track impact |
| **Beneficiary** | `beneficiary@papama.test` | registration flow only | No dashboard; used for the beneficiary register/redeem path |

## Notes
- Admin, Compliance and Vendor Manager all sign in at the same `/login` page;
  the console adapts to the role.
- The donor flow can also be tried **without an account** via the public
  `/donate` page.

---
*Generated by `scripts/seed-demo-users.mjs`. Re-run that script to (re)create or
reset these accounts.*
