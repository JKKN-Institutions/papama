# Normalize line endings: store everything as LF in the repo,
# check out with native line endings on each OS.
* text=auto eol=lf

# Binary files: never touch these (no line-ending conversion)
*.png binary
*.jpg binary
*.jpeg binary
*.gif binary
*.webp binary
*.ico binary
*.woff binary
*.woff2 binary
*.pdf binary